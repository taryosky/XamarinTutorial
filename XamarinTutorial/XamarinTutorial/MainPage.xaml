<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinTutorial.MainPage">
    <ContentPage.Content>

        <StackLayout Margin="10,20" VerticalOptions="Center">
            <Image x:Name="iconImage" Aspect="AspectFit" HorizontalOptions="FillAndExpand" Margin="0,0,0,50" />
            <Entry x:Name="email" Placeholder="Username" Keyboard="Email"/>
            <Entry x:Name="password" Placeholder="Password" IsPassword="True"/>
            <Button x:Name="LoginButton" Clicked="LoginButton_Clicked_1" Margin="20" BackgroundColor="{StaticResource primaryColor}" Text="Login" Style="{StaticResource normalButton}"/>
        </StackLayout>
    </ContentPage.Content>
    

</ContentPage>

